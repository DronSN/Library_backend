server :
  port: 8888
spring :
  liquibase :
    change-log: classpath*:migrations/master-changelog.yml
  datasource:
    url     : jdbc:postgresql://localhost:5433/library
    password: greentea
    username: postgres
mybatis:

  type-handlers-package: ru.relex.library.db.handlers
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
